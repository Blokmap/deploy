apiVersion: v1
kind: PersistentVolume
metadata:
    name: backend-db
    labels:
        app: blokmap
        environment: staging
        tier: backend
        component: volume
        related-to: database
spec:
    storageClassName: manual
    capacity:
        storage: 50Gi
    accessModes:
        - ReadWriteOncePod
    hostPath:
        path: "/home/tibo/blokmap-data"
