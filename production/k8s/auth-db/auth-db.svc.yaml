apiVersion: v1
kind: Service
metadata:
    name: auth-db
spec:
    clusterIP: None
    selector:
        app: blokmap
        environment: staging
        tier: auth
        component: database
    ports:
      - protocol: TCP
        port: 5432
        targetPort: 5432
